@*-
Title: Test page
DestinationPath: test/index.html
IncludeInSitemap: false
Description: Test page, do not follow!
-*@
@inject CommonService common
@inject GithubService github

<h1>Test</h1>

<section>

    <p>This is a test page!</p>

    <p>Site URL: @(await common.Probe())</p>

    <p>Host: @settings.GetString("host")</p>

    <p>@(Document.Get(WebKeys.IncludeInSitemap, true) ? "Public" : "Private")</p>

</section>

